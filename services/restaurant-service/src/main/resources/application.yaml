spring:
    application:
        name: restaurant-service
    datasource:
        url: jdbc:mariadb://localhost:3306/zeno_eats_db
        username: zeno_app_user
        password: w3P02V7>|TU?|}}b
        driver-class-name: org.mariadb.jdbc.Driver
    jpa:
        show-sql: true
        database-platform: org.hibernate.dialect.MariaDBDialect
        properties:
            hibernate:
                ddl-auto: update

server:
  port: 8082
